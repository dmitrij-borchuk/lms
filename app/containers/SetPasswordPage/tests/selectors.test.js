// import { fromJS } from 'immutable';
// import { makeSelectSetPasswordPageDomain } from '../selectors';

// const selector = makeSelectSetPasswordPageDomain();

describe('makeSelectSetPasswordPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
